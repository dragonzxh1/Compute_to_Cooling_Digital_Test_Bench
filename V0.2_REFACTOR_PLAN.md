# C2C-DTB V0.2 重构计划与 V0.1 审查

Revision: 1

Status: PHASE 1 Revised — Awaiting User Approval Before PHASE 2

日期：2026-09-16。本文为计划修订，不是 V0.2 已实现声明。

审查基线：`62c0220e8e8b7edffc5ef59c8dfaa282f4d1c812`，包版本 `0.1.0`。
本次唯一交付为修订本文档；不修改 V0.1 源码、配置、测试、结果或历史报告，不创建分支或实现模块，不开始 PHASE 2–9，不推送本计划。

## Revision History

| Revision | Date | Description |
|---|---|---|
| 0 | 2026-09-16 | Initial V0.2 refactor plan |
| 1 | 2026-09-16 | Technical review corrections |

Revision 1 变更：thermal split reworked；flow-dependent cold plate model added；thermal-matched benchmark added；energy-matched benchmark added；performance/safety benchmark separated；DCGM control/monitoring paths separated；numerical integration strategy added；pump energy boundary clarified；FWS disturbance added；branch hydraulic disturbance added；measured-disturbance feedforward formally defined。另补充过冷指标、P–Z 验收和 PHASE 2 的 16 项合同门禁。

第 1–4 节中的 V0.1 代码行为、结果与参数属于 Revision 0 的审查证据，保留作历史基线；第 2.5–2.7 节及后续设计是 V0.2 规划，不能互相混用。Revision 1 不重新宣称硬件字段或测试已获新验证。

## 1. 当前 V0.1 架构

定位符合 **GB300 Load-to-Cooling Feedforward Control Proof-of-Concept**。现有代码不能据此认定为经过真实 GB300/CDU 校准的 Rack Physical + Control Digital Twin。

V0.2 目标保持 **GB300 Rack Liquid Cooling Physical + Control Digital Twin**；实现验证与具体设备校准验证分别报告。

审查范围：`src/c2c/` 全部功能域、默认场景和流体配置、`tests/`、项目说明与架构/物理/假设文档、打包和 CI。API、DPS、DSX、DCGM 占位边界也包含在范围内；没有将历史参考项目的能力当作本仓库能力。

当前调用链：

```text
CLI runner → load_scenario → run_benchmark
  ├─ feedback_only
  └─ guarded_feedforward
每个工况：
SyntheticWorkloadSource → ComputeTelemetry → ComputeToHeatModel
                                            ├─ 四节点聚合 RC
                                            └─ ShadowLCI → SupervisoryIntent
上一时刻供液温度/压差 → VirtualPLC → 泵/阀指令
    → PumpSystem 代数工作点 → 回水温度 FIFO → CDU epsilon-NTU
    → 下一时刻供液边界 → CSV / JSON / 中英文 HTML / 图表
```

| 代码位置 | 已实现 | 尚未实现或未接入 |
|---|---|---|
| `workloads/synthetic.py` | 分段利用率生成聚合 GPU 功率 | 独立设备功率、连续 ramp/burst/random 工厂 |
| `workloads/replay.py` | CSV 最近时间点查找 | 引擎注入、因果回放、JSON、缺测策略 |
| `compute/` | 电功率合计、液冷/空气热分流 | 动态 CPU/board 功率、计量去重 |
| `thermal/` | 四节点 RC、分数 FIFO 温度延迟 | GPU/CPU 独立路径、管路质量/焓库存 |
| `hydraulics/` | 二次泵曲线与集中阻力 | 分支网络、阀 Cv、动态执行器 |
| `cdu/` | epsilon-NTU 换热、恒定 FWS 边界 | CDU 储能、容量铭牌、FWS 动态、冗余 |
| `controls/` | 两个 PID、建议接受/限幅/渐变/超时回退 | 三模式、流量环、外层温控、完整联锁 |
| `adapters/` | Exporter 名称映射、DCGM/DPS/DSX 接口占位 | 活体数据接入、采样质量与版本协商 |
| `reports/` | 阈值、热/泵能耗、响应时间线、中英文图表 | 全系统守恒、完整 KPI、自动公平性门禁 |
| `api/app.py` | `/health` | 仿真任务服务或现场控制接口 |

所有物理、PLC、日志更新共用 `SimulationClock`；LCI 仅通过时间差实现较慢更新。默认物理步长 1s、LCI 周期 5s，不是独立的多时钟事件调度。

Revision 0 验证：`.venv/Scripts/python.exe -m pytest -W error` 得到 **72 passed**；`ruff check .` 通过。测试证明当前实现符合已有断言，不证明满足 V0.2 的全部要求。Revision 1 仅做文档自审，不将旧测试结果当作新增设计的验证。

## 2. 当前物理模型

### 2.1 Power → Heat

`compute/heat.py` 的热输入确实是功率，而不是直接把利用率当瓦特：

**下面固定比例热分流仅描述 V0.1；V0.2 GPU/CPU 主动态模型使用第 2.5 节的热网络。**

```text
P_IT = P_GPU + P_CPU + P_other
Q_liquid = α_GPU P_GPU + α_CPU P_CPU + α_other P_other
Q_air = P_IT − Q_liquid
```

但 `workloads/synthetic.py` 默认上游仍使用 `P_GPU=N×min(P_limit,P_idle+util×(P_max−P_idle))`。CPU/other 是配置常数。引擎直接实例化 SyntheticWorkloadSource，没有数据源选择器。因此只能说具备功率输入边界，不能说已摆脱利用率映射或已接实测数据。

### 2.2 RC 与冷却液

`thermal/rc_network.py`：die → package → cold plate → lumped coolant 四节点显式 Euler；内部导热通量成对抵消，单步节点储能守恒已有测试。步长上限同时考虑相邻导热通道和冷却液平流，值得保留其验证思想。

整个 `Q_liquid`（含 CPU/other）注入 GPU 命名的 die 节点，因此温度是**聚合等效热点**，不是单颗 GPU 结温。没有 device/tray/branch 拓扑。`steady_initial_state` 只求给定供液/流量下的机架 RC 初态，不是 CDU 与机架联合稳态。

`thermal/transport.py` 使用固定秒数的分数 FIFO：支持温度信号延迟，但没有明确管内容积、固定质量及变流量时的焓输运，不能作为全回路能量账本。

### 2.3 Hydraulic 与 CDU

`PumpSystem` 解二次泵曲线和 `ΔP=KQ²` 的工作点，流量随转速、压差随转速平方、功率随转速立方变化；**不是线性泵功率模型**，已有 affinity-law 测试。

`CDUPlant` 不是无限制理想冷源：已有 epsilon-NTU 和两侧热容量流率约束。但它是瞬时代数 HX，主侧流量直接与阀位成正比；无独立额定容量约束、板换金属/水容积、阀特性、VFD 滞后与 FWS 动态。较热的 FWS 当前只使换热降为零，不表示已经建成双向换热模型。

### 2.4 守恒口径必须先定清楚

按用户要求保留 `raw_IT_water_gap = |P_IT−Q_water|/max(P_IT,ε_W)`，并输出瞬时、时间加权平均、P95、最大值。但它不是普遍适用的瞬态守恒残差：部分热走空气侧，芯片和水会储热，泵耗也可能进入液体。

V0.2 至少同时输出：

```text
Q_water,rack = Σ_branch m_dot × (h_return − h_supply)
R_rack = P_IT − Q_air − Q_water,rack − dE_rack/dt
R_loop = P_IT + Q_pump_to_liquid − Q_air − Q_FWS − Q_ambient − dE_total/dt
R_integrated = ΔE_total − ∫(P_IT + Q_pump_to_liquid − Q_air − Q_FWS − Q_ambient)dt
R_mass,node = Σm_in − Σm_out − dM_node/dt
```

控制体、功率计量边界和泵热归属必须写入元数据；同一界面流量/焓通量由单一计算结果供相邻控制体共享。用同一时间离散的积分残差做数值守恒门禁，防止将采样错位当成物理问题。近零输入用绝对 W/J 容差，同时报告归一化尺度。

上述 `E_rack` 包含 rack 设备、plate 与本地水库存；`E_total` 包含设备、plate、所建 TCS 管路和 CDU 热库存。若 rack air 作为动态内部节点，其存热计入 E，`Q_air` 必须取最终穿出控制体的净空气侧热流，而非再计一次 device→air 的内部交换。`Q_ambient` 只计未包含在 `Q_air` 的管壁等外部热流，避免重复；FWS 接口是该热控制体的出口。`Q_pump_to_liquid` 按第 2.7 节定义，不能简单等于泵电功率。

默认高负载下 P_IT=100.8kW、Q_liquid=91.416kW、Q_air=9.384kW；即使完全稳态，直接用总 IT 与水热比较也有约 9.31% 的空气侧差额，不能用强制归零来“修好”模型。

### 2.5 V0.2 Power → Thermal Network → Liquid / Air

GPU/CPU 去重后的全部设备电功率首先进入对应热节点，不先乘固定 liquid capture fraction。示意路径为 `P_device → die capacitance → package/TIM → cold plate → local coolant`，并允许 die/package/board 经 `R_to_air` 向 rack air/ambient 散热。节点方程采用有符号通量：

```text
C_i dT_i/dt = P_i + Σ_j G_ij(T_j−T_i) − Q_i,to_boundary
P_device = dE_device/dt + Q_to_liquid + Q_to_air + Q_other_explicit
Q_to_liquid = Σ plate-to-coolant interface heat flux
Q_to_air = Σ explicit air-path boundary flux (or air-node balance)
```

内部边界通量只算一次、在相邻控制体中符号相反。液冷/空气热量由热阻、热容、流量和温差自然形成；功率突变初期允许储热，不能强制两种散热功率之和瞬间等于 P_device。每个节点和路径的单位、方向、有效范围、初态及来源都配置化。

Liquid Capture Ratio 保留为：①合格稳态窗口的校准/验证指标；②明确独立 board/other 散热域的分配参数；③缺详细结构时显式标注的 Generic approximation。瞬态 `Q_liquid/P_device` 可能因释放历史储热超过 1，不能因此裁剪能量；稳态或积分比值必须说明时间窗和储能修正。GPU/CPU 核心路径不得用 capture ratio 替代 R/C；FF estimator 可用独立标定近似估计液冷需求，但它的估计值不得强制驱动物理 plant 的真实热分流。

`power_domain` 表示独立电计量域，`thermal_domain` 表示功率注入/储热域，`included_in` 表示测量覆盖关系，`cooling_path` 表示 liquid/air/mixed 的连接拓扑。VRM、NIC、NVSwitch、DIMM/HBM、fans、PSU losses 按实际域映射；HBM 或 VRM 已被 board power 覆盖时不得再相加。Mixed 域只注入一次功率、由多个热路径分出；Generic split 的权重必须和为 1 且仅用于明确声明的近似域。PSU/fan 是否进入模型取决于计量边界，不能既含在 rack 总表又作为额外输入。

### 2.6 Flow-dependent Cold Plate Performance

每块 GPU/CPU cold plate 使用其**本地支路质量流量**计算 `R_cp(m_dot)` 或 `UA_cp(m_dot)`；优先采用厂家指定 coolant/浓度、入口温度、热负荷与 TIM/contact 条件下的曲线。Generic 模型可配置为：

```text
h(m_dot) = h_ref × (m_dot / m_dot_ref)^n
R_cp(m_dot) = R_conduction + 1 / (h(m_dot) × A_effective)
UA_cp = 1 / R_cp       # 仅当 R_cp 指同一对端点的等效热阻
```

`h_ref, m_dot_ref, n, A_effective, R_conduction>0, valid_flow_range` 全部外置并标 ENGINEERING_ASSUMPTION。h 的换热表面 UA 与含固体导热的整体 UA 不能混用；device-to-coolant 总 Rth 若已含 TIM/contact，不可再串联重复 TIM。

有效域内增流应合理降低热阻，极限不低于 `R_conduction`，不能无限改善。超出流量/温度/工质适用域产生 `MODEL_OUT_OF_RANGE`，该工况不得作为经验证的效率结论；正常资格验证禁止静默外推。零流量单独采用明确、有限的静止流体导热/自然对流边界或声明该模型无法继续，不能用除零、无限 UA 或虚构最小流量掩盖故障。保护研究若采用域外保守模型，须显式标记并保留未验证状态。

新增测试合同：厂家曲线点重现；有效域内 Rth 随 flow 单调非增且有下界；固定入口温度/热输入下温度响应合理；阻力热性能与 ΔP 曲线使用同一工况；min/max 边界、域外报警、零流量政策、局部失流与支路不均衡；所有热通量仍纳入储能守恒。

### 2.7 Pump Electrical / Hydraulic / Thermal Boundary

```text
P_pump_electrical = VFD 输入端的泵驱动链总电功率
P_pump_hydraulic = ΔP_pump × volumetric_flow
η_total = P_pump_hydraulic / P_pump_electrical
P_loss = P_pump_electrical − P_pump_hydraulic
P_loss = P_vfd_loss + P_motor_loss + P_pump_internal_loss
```

VFD、motor 是串联转换环节，不将各级输入功率相加。ΔP 取泵进出口总扬程的约定等效压差，不能只用某一支路压差代替；无动压/位能差的简化需声明。零电功率时 η 标 N/A；η>1、负损耗或不支持的泵曲线工况为无效，不通过裁值隐藏。

每类损耗分配系数均在 [0,1]，同一损耗的 liquid/ambient 分量和为 1：

- `motor_loss_to_liquid_fraction` 与 `motor_loss_to_ambient_fraction` 作用于 P_motor_loss。
- `vfd_loss_to_ambient_fraction` 作用于 P_vfd_loss，其补量只有明确液冷 VFD 时才进入水侧。
- `pump_internal_loss_to_liquid_fraction` 及其 ambient 补量作用于泵内机械/水力损耗。

仅有总效率时无法唯一分离这些损耗，必须采用有来源的 Generic 分配并标 CALIBRATION_REQUIRED，不能假定全进水。

泵提供的液压功在管路/阀/冷板阻力上耗散，也要记账。V0.2 准稳态闭合回路默认在各阻力元件将 `ΔP_element × Q_element` 转为热：网络耗散之和 `D_hyd≈P_pump_hydraulic`；采用动态机械储能时改为 `P_hyd=D_hyd+dE_mechanical/dt`，同步扩展总能量库存。热控制体中的 `Q_pump_to_liquid` 是**各类损耗进入液体的部分 + 水力耗散进入液体的部分**，不得再额外添加一次 P_hyd 或 P_electrical。若部分耗散进入壁/空气，走独立路径。

审计同时验证 `P_electrical = Q_drive,liquid + Q_drive,ambient + D_hyd + dE_mechanical/dt`（驱动链热惯性若不忽略则另加其储能），以及液体/壁/空气热流分配闭合。设施电力报表积分 P_electrical；冷却液热报表积分实际入水热流，二者不是两份设施电耗。FWS 冷却电耗另有模型才计入，且检查计量边界是否已含泵/VFD。

报告分别输出 Pump Electrical Power、Pump Hydraulic Power、Pump Efficiency、Pump Heat to Liquid、Cumulative Pump Electrical Energy；泵亲和律保留，但不得用单一三次曲线冒充真实电机/VFD 效率图。

## 3. 当前控制逻辑

`ShadowLCI.recommend` 依据当前液冷热功率算 `m_dot=Q/(Cp ΔT_target)`，再用集中阻力换算压差目标。供液目标由 `30.5−4.5×clamp(Q/110000,0,1.2)` 给出；这些策略常数没有全部外置，也没有以最大安全供液温度为目标。

`VirtualPLC` 接受高层 intent；压差 PID 控泵，供液温度 PID 控一次侧阀。已有指令上下限、执行器 ramp、温度目标渐变、intent 超时/未来时间/非有限数校验及无效传感器保持。PLC 最终执行权结构可以保留。

与 V0.2 的差异：

- FF 现在改变 PID 设定值，尚无独立 `u_FF` 与 `u_FB` 分量账本，也无 FF-only 模式。
- 没有流量 PID、外层热裕量/温度反馈调整压差的级联环；仅反馈采用固定压差目标。
- `intent.timestamp_s` 是建议生成时间，不能代替原始遥测采样时间。未来适配器若反复给旧测量盖新时间，会绕过现有 watchdog。
- 温度/压差输入只是上一周期模型输出，不包含独立传感器采样、偏置、噪声和通信延迟。新增的 `plc_measured_*` 名称不代表已模拟真实测量链。
- PID anti-windup 只考虑内部输出上下限，不反馈下游 ramp/delay/联锁后的实际动作；V0.2 要用 actuator tracking 防止积累。
- `throttle` 只是越温布尔记录，不会实际降低 IT 功率或触发保护状态机。

当前默认结果（Revision 0 重跑证据）：

| 指标 | 仅反馈 | FF + PID |
|---|---:|---:|
| 聚合等效热点峰值 | 81.538°C | 77.972°C |
| 泵能耗 | 0.124554kWh | 0.137359kWh |
| 当前末段机架热平衡误差 | 2.040% | 0.802% |
| 阀位首次变化 2 个百分点 | 104s | 5s |
| 相对最终温度目标最大偏差 | 2.909K | 3.261K |

前馈泵能耗增加 **10.28%**，不能把更低温度直接解释为节能。当前两组硬件配置相同，但没有自动 FAIR_COMPARISON 验证；固定压差基线也不足以代表所有传统 PLC 级联策略。

## 4. 当前参数与来源

默认 `configs/scenarios/workload_step.yaml` 有 **61 个带来源记录的参数：59 ASSUMED、2 LITERATURE**。本文件中没有官方或测量参数。time、phase、seed 等普通字段也未全部带来源结构。

| 参数组 | 当前值举例 | 解释 |
|---|---|---|
| GPU 功率 | N=72；idle=200W/GPU；max/limit=1200W/GPU | 现有值均标 ASSUMED；不能从官方 GPU 数量推导功耗正确 |
| CPU / other | 12000W / 6000W | 整架常数，非 Grace 逐设备实测 |
| 液冷捕获率 | 0.97 / 0.75 / 0.35 | 需按实际计量边界和冷却拓扑标定 |
| 热容 | 18000 / 108000 / 500000 / 1250000 J/K | 聚合值，不能原样复制成单 GPU 参数 |
| 热阻 | 0.00028 / 0.00014 / 0.00010 K/W | 聚合并联等效值；需重新确定设备级 R、C |
| 流体 | ρ=995kg/m³；Cp=4180J/(kg·K) | LITERATURE 近似；PG25 配置也只是近似值 |
| 温度/输运 | throttle=90°C；delay=18s | 工程阈值与估算延迟，不是 NVIDIA 限值 |
| 水力 | K_system=2e9；K_pump=5e8；shutoff=130000Pa；η=0.72 | 集中阻力、定效率 |
| HX/FWS | UA=18000W/K；20°C；primary max=0.007m³/s | 未经 FAT 标定；文档称“1MW-class”不能视为模型认证容量 |
| PLC | 60kPa、30°C；泵 25–100%；阀 5–100%；ramp 2/1.5百分点每秒 | 参数均为假设；目标限幅不等于实际压力/流量保护 |
| 目标/采样 | 0.1K/s 温度目标渐变；LCI 5s；intent timeout 15s | 无独立遥测/网络/执行器延迟 |
| 报告 | COP=8；供液跟踪3K；末段热平衡3% | 比较用假设，非厂商验收 |

V0.2 采用参数记录：`name, value, unit, source_type, source_ref, confidence, date, valid_range, calibration_status`。来源分类包含用户要求的 OFFICIAL / DATASHEET / LITERATURE / ENGINEERING_ASSUMPTION；CALIBRATION_REQUIRED 可作为明确状态，避免把“待标定”误当成来源。保留 MEASURED/CALIBRATED 证据链接。配置数据不是官方数据，Synthetic 是数据生成方式，应与参数来源分开标识。

## 5. 不合理之处与优先级

| 优先级 | 证据/问题 | 影响与 V0.2 对策 |
|---|---|---|
| P0 | `thermal/transport.py` 无质量/能量库存 | 以有限体积焓输运替代闭环物理 FIFO，先验证闭环积分守恒 |
| P0 | `engine.py` 用新 flow/supply 算 `coolant_heat_removed`，RC 本步用旧边界积分 | 报告值不能直接作为同区间能量审计；统一界面通量与时间边界 |
| P0 | `workloads/replay.py` 最近邻 `argmin` 可读取未来样本 | 改为 sample_time≤now 的因果 hold；过期数据置质量状态，不延长寿命 |
| P0 | `VirtualPLC` 限定目标但缺实际 min/max flow、max ΔP、联锁与冗余状态 | 实施独立 safety supervisor；性能实验只记录降载请求，保护实验才执行降载；禁止篡改物理状态满足限制 |
| P0 | V0.1 先按 capture ratio 分流，cold plate R 固定 | GPU/CPU 全功率注入热网络，显式空气路径；cold plate Rth/UA 随本地 flow 变化并限制有效域 |
| P1 | GPU/CPU/other 共用“GPU”热节点 | 分设备/热域建模，不将 aggregate 温度称为结温 |
| P1 | `ShadowLCI` 与 plant 共享精确热输入及水力参数 | estimator 使用延迟/有噪测量与独立标定参数；增加模型失配测试 |
| P1 | 泵/阀只有 ramp，无 VFD 动态，valve→flow 线性 | 添加 delay、lag、Cv、pump/η map 与容量约束 |
| P1 | 没有设备、tray、branch 清单 | 建拓扑、质量流分配与局部最坏温度检查 |
| P1 | 两模式、固定 ΔP 基线、无自动公平门禁 | 同一个 PLC/硬件/测量管线；Benchmark A 只切换 FF，B/C 的外层匹配参数仅按预注册白名单变化 |
| P1 | steady window 仅按时间截取 | 加 dE/dt、温度/流量斜率及最短持续时间门禁；未稳态不能标稳态 PASS |
| P1 | 工况至少两 phase、指标依赖第2/3段 | 将求解器与事件/指标窗解耦，支持 burst/random/replay |
| P2 | `benchmark_summary` 假定两工况存在；主配置验证未覆盖全部温度边界的有限性 | V0.2 schema/运行状态统一校验，缺列缺工况显式 INVALID |
| P2 | 步间标准差随 dt；throttle 只计样本数 | 控制努力用总变差、rate 用百分点/s；越限报告次数和持续秒数 |
| P2 | `.tmp-*`/`results` 被忽略，Git 不保存历史报告 | 后续隔离前对 V0.1 报告做 manifest/哈希备份；本阶段不搬移或覆盖 |

`cold_plate_heat_w` 有单向截断且未接入主 RC；不能直接拿它作为可逆节点通量。HX 现有核心在常规冷却域可复用，但需明确反向温差政策和加入两侧守恒测试。

## 6. 可以保留的代码

保留 V0.1 原包、CLI、配置、测试和报告。V0.2 对可用代码采用受控迁移，不修改原文件共享实现。

| 可保留对象 | 复用条件 |
|---|---|
| `cdu/heat_exchanger.py` epsilon-NTU 内核 | 补双侧焓闭合、零流量、容量/逆温差边界测试 |
| `hydraulics/pump.py` affinity 与二次曲线数学 | 保留单泵参考测试；扩展网络、效率图和有效域 |
| `core/units.py`、provenance 结构思想 | 统一 SI；单位转换在边界完成；扩展来源与校准证据 |
| PID/guardrails 的局部算法 | 重置/无扰切换、实际执行器 tracking、不同周期测试 |
| RC 成对导热和稳定性检查 | 作为单元参考；不复用整架聚合拓扑和参数 |
| HTML/Agg/i18n、CSV/JSON 导出 | 新报告版本与目录隔离；继续无 GUI 环境、中文字体、wheel 验证 |
| 72 项 V0.1 测试 | 独立 legacy 回归，防止新打包/依赖间接破坏旧入口 |

## 7. 必须重写或实质扩展的代码

1. **数据契约与调度器**：逐设备 source/measurement/receive/available 时间、质量、来源、单位；source 可注入；多周期延迟队列。
2. **Power/thermal/rack/pipe**：去重功率域、GPU/CPU 全功率注入节点、显式空气散热、flow-dependent cold plate、local coolant、tray/branch/manifold 及固定管路库存。
3. **控制与安全**：热需求估计、FF 与 PID 明确分量、三模式、级联环、watchdog、实际状态联锁、执行器动态和可行性检查。
4. **评估器**：守恒账本、公平性 gate、所有要求的 KPI/工况/图表；区分数值正确性、控制约束、安全和校准可信度。

DCGM stub 不是现成功能；需要新适配器。DPS/DSX/API 保留边界但不作为本期依赖，不引入 AI/ML、RL、CFD、复杂 MPC 或界面工程。

## 8. V0.2 模块结构与关键合同

审查通过后，从上述 commit 新建 `codex/v0.2-physical-twin`，推荐在独立 worktree 开发；新增独立包目录与命令，不替换 `c2c` 或 `c2c-dtb`。V0.1 基线 SHA、配置、报告哈希及依赖环境形成保留清单，Git tag/归档在执行阶段完成。本次不创建分支或开始实现。

```text
c2c_dtb_v02/
  README.md / pyproject.toml
  config/{gb300,rack,cdu_generic,control,telemetry,scenarios}.yaml
  models/{chip_thermal,coldplate,coolant,hydraulic,rack,cdu,heat_exchanger}.py
  telemetry/{base,synthetic_adapter,replay_adapter,dcgm_adapter,dcgm_exporter_adapter}.py
  control/{pid,feedforward,supervisor,safety}.py
  simulation/{engine,scenario,timestep,thermal_integrator}.py
  metrics/{thermal,energy,control,physics,fairness}.py
  calibration/{schema,loader}.py
  reports/generator.py
  tests/  examples/
reports/v0.2/<run_id>/
  01_summary.md ... 06_assumptions.md
  C2C_DTB_V0.2_Report.html
  timeseries.csv / device_states.csv / branch_states.csv
  config_snapshot.yaml / run_manifest.json / acceptance.json / figures/
```

### 数据流与控制流

```text
Scenario/Replay → true per-device power → Physical Plant → true states
                     ↓                           ↓
             IT measurement pipeline    liquid/device sensor pipeline
                     ↓                           ↓
             ThermalDemandEstimator     PLC feedback / safety
                     ↓                           ↓
             FF recommended intent ──→ Supervisor → PLC → delayed/ramped actuators
```

GPU 温度属于反馈；utilization 为可选辅助量，不用于替代已有功率测量。Board/Module 功率可能包含 GPU/CPU/HBM，必须有 power-domain 覆盖关系及 `included_in` 去重规则。若 `P_other=k_other×P_total` 而 total 未测得，则使用 `(P_GPU+P_CPU)/(1−k_other)` 并校验 k 范围；不可循环定义或重复计热。

### Thermal / hydraulic flow

```text
GPU / CPU full power → die/package RC → TIM/plate(flow) → local coolant
                              └─ R_to_air → rack air / ambient
local coolant → tray → branch return
                                  ↑                   ↓
CDU supply → supply pipe → manifold → parallel branches
                                  ← return manifold ← return pipe
CDU secondary thermal volume → finite HX → primary/FWS boundary
```

GPU/CPU 设备可配置分组等效，但必须保留最热组及每支路差异。并联支路共享压差、`ΣQ_branch=Q_total`；泵曲线与网络联立求解。热不平衡不必在固定阻力模型里自动导致流量不平衡；分开测试等功率不均匀、阻力不均匀和混合故障。

固定管路体积分段建模 `M_i dh_i/dt = m_dot(h_up−h_i)+Q_i`；变流量不能凭空改变库存。`transport_delay_seconds` 定义为标称流量下的 residence time，用它和 nominal flow 推导等效体积；若同时给体积则校验一致性。实际延迟随流量改变。不可再叠一个同样的热 FIFO 重复延迟。

### 三控制模式与最终执行权

推荐统一到压差目标这一物理量：

```text
ΔP_requested = ΔP_base + ΔP_FF + ΔP_FB_outer
PLC: ΔP tracking PID → VFD command → interlocks → delay/ramp → pump
Supply-temperature PID → guarded valve command
```

FF-only 关闭研究层反馈修正，但保留 PLC 内环、硬保护与 watchdog；报告明确这一语义，不将它称为无任何闭环。PID-on-flow 可替代 ΔP 内环，两者不同时争夺同一泵指令。若使用 pump-bias 模式，则 FF/FB 必须同为百分点后求和，不能把 kPa 与百分比相加。

FF 只访问 IT 测量与配置估计器，FB 访问测量温度/流量/压力；控制器不得读取 plant true-state。输出 requested/accepted/commanded/actual、FF/FB 分量、限幅原因和状态机事件。thermal safety 不能仅用“hold last value”代表；流量不够或容量不足时报警/转备用/请求负载降额，并记录未能满足的物理约束。请求与实际降载分离：第 9 节 Performance Benchmark 禁止实际改动 IT trace，只有 Safety / Protection Benchmark 允许保护状态机改变 Synthetic 实际功率。

最大安全供液温度采用简单外层规则：在最差器件裕量足够、最小流量/最大压差/容量可行且无故障时缓慢提高目标，否则降低或冻结；上下界、死区、速率、露点约束参数化。没有 FWS 效率模型时，不据此声称 PUE 收益。

正式目标是在 thermal limit、minimum headroom、flow、pressure、CDU capacity 和 actuator constraints 可行前提下，减少泵电能、control effort 和不必要过冷，并尽可能提高 safe supply temperature。采用约束优先的多目标/Pareto 比较，不用“GPU 越冷越好”作为单一奖励。`Thermal Margin Excess_i=max(Headroom_i−RequiredHeadroom_i,0)`；`Excess Cooling Integral=∫Σw_i×ThermalMarginExcess_i dt`（w_i≥0、Σw_i=1，单位 K·s，同时报告逐器件与最坏/平均统计）。它是过剩温度裕量指标，不是浪费电能的直接证明；需与泵能耗、约束和工作负载联合解释。

### Measured-Disturbance Feedforward 与输入权限

V0.2 正式采用 **Measured-Disturbance Feedforward**：GPU/CPU 功率已经变化，系统通过可用的 IT 测量估计热需求，在芯片/回水尚未充分响应时调整 Flow / ΔP / Supply-T 建议。它不要求预测 `P(t+30s)`，也不保证在所有采样/执行器条件下都领先反馈。估计器可以有因果的确定性热状态，但不可读未来 trace 或 plant 真值。

V0.3 才考虑 Predictive Feedforward：scheduler、job metadata、inference queue、workload state、power/utilization trend 等用于估计未来负载；V0.2 不实现 ML、任务预测或将结温当主要 FF 输入。

| 接收方 | 允许输入 | 禁止输入/职责 |
|---|---|---|
| Thermal estimator / FF | 已 available 的 IT 功率、source quality/age、静态标定和拓扑 | plant truth、未来功率、未来 FWS/堵塞事件日程、GPU 温度驱动主要 FF |
| Thermal outer FB / PLC | available 的 GPU/水温、flow、ΔP、经过检查的 intent | plant truth、未来扰动日程；不得越过 safety |
| Safety supervisor | 相同测量链/明确模拟的独立硬保护通道、设备状态、限值 | 不能获得未建模的完美传感器；不能绕过性能实验的 trace 锁 |
| Plant / scenario injector | IT 真值、FWS/支路事件日程、实际执行器 | 不把隐藏扰动日程传给 FF |
| Metrics / fairness auditor | 全部真值、测量、事件和哈希 | 只做评价，不向控制器回灌真值 |

### Telemetry 四路径与时间戳合同

| Adapter | 角色 | 数据可用性条件 |
|---|---|---|
| Direct DCGM / Hostengine (`dcgm_adapter`) | 低延迟 control telemetry、保留时间戳的 recorded control trace | 按设备支持/源时间戳验证实际刷新率，不保证“direct”必然快于物理传感器 |
| DCGM Exporter (`dcgm_exporter_adapter`) | monitoring、Prometheus、observability、historical replay、integration testing | 非默认唯一快速控制入口；只有经验证 freshness 合同才可用于控制实验 |
| Synthetic | IT 真值生成后经统一测量链提供记录 | 所有延迟/噪声/时间戳规则与其他 source 一致 |
| Recorded Replay | 因果 CSV/JSON trace | 不读取未来样本，明确源时钟/回放时钟转换与 missing/stale |

统一 `TelemetryRecord` 至少包含 `schema_version, source_type, source_version, entity_type, device_uuid, rack_id, tray_id, branch_id, sample_time, receive_time, available_time, value, unit, quality, sequence_id, averaging_window, power_domain, thermal_domain`，另带 metric_name、timebase/clock uncertainty 及需要时的 `included_in`、`cooling_path` 引用。

`Prometheus scrape interval ≠ GPU hardware measurement interval`；`Exporter value refresh ≠ 新 GPU sample`。不能用 controller receive time 给旧样本重新计龄；重复抓取保留源 sample_time/sequence_id。源时间戳无法取得时标 `TIMESTAMP_UNKNOWN`，不得声称鲜活低延迟数据；仅在预定义保守 age 上界且满足 watchdog 时启用控制，否则保持 monitoring/回退。sample/receive/available 统一到显式时钟，校验因果顺序并保留时钟误差。

PHASE 2 只定义四路径与记录合同；PHASE 7 实现完整 delay、sampling、staleness、noise、sentinel、packet loss。保留官方字段版本核验，不在本次写 adapter。

### 多周期与真实性

`physics_dt, telemetry_dt, controller_dt, PLC_dt, actuator_dt` 相互独立。物理默认候选 dt=0.1s，可细分；IT 采样支持 0.1/0.5/1s。以整数 tick 或事件时间调度处理非整数倍，禁止 `int(period/dt)` 静默截断。先把物理状态推进至下一事件时刻 t（只用已生效的输入），再对该真值采样/交付到期数据，随后执行到期 controller、PLC 和 actuator 事件，得到下一物理区间的输入。零延迟事件的先后优先级写入合同。采样、接收、控制可用、命令生效及日志区间左右端定义都保留。

温度、功率、流量、压力均有独立 noise/bias/sample-and-hold；测量上报周期不等于硬件内部功率平均窗口。每类传感器独立随机流，以固定 sample index 生成噪声；三模式共享同一扰动序列，不能因控制分支多调用 RNG 改变输入公平性。

watchdog 同时检查原始 sample age、丢包、NaN/Inf、模型失败/置信度、拓扑功率覆盖率与可行域；FF 故障无扰撤出，PLC 本地控制持续运行，恢复需连续有效样本/滞回。对泵故障和传感器故障，分别定义 N+1 切换或无备用降级路径，不能默认存在冗余硬件。

### Numerical Integration Strategy

PHASE 2 定义 `ThermalIntegrator` 合同：输入节点能量/温度状态、同区间 source/boundary、flow-dependent conductance、开始时刻和步长；返回下一状态、每个界面的**区间积分热量**、能量残差、收敛/有效域状态。积分器不得隐藏 clipping 或修改功率守恒；plant/control 的周期不得因更换积分器而同步改变。

| 方法 | 用途与限制 | 计划选择 |
|---|---|---|
| Explicit Euler | 透明的解析参考；必须满足全部 RC 稳定/正性约束及显式 advection CFL | 单元参考/小步长对照，不默认承担刚性 device 网络 |
| Semi-implicit / implicit | 耦合热节点可减轻 stiffness；隐式 advection 仍有数值扩散，稳定不等于准确 | **默认候选为 conservative backward Euler**；非线性 h(T,flow) 迭代/冻结策略及残差需受控 |
| Exact linear RC discretization | 常系数、分段恒定输入时用矩阵指数给精确参考 | 线性子网解析验证；不能声称对变流量、非线性和切换系统全局精确 |

默认候选选择依据是 device R/C 时间尺度差异及守恒审计需求；只在 PHASE 3 解析、守恒和收敛验证通过后接受。Implicit 求解失败必须报错/缩小物理子步并记录，不能静默继续；所有界面交换使用一致积分通量。恒 Cp 与温变 Cp 的状态变量分别明确为 C·T 或积分焓，禁止把变化热容简单乘 T 当能量。

强制 `dt convergence test`：每个资格工况在 nominal `physics_dt=0.2/0.1/0.05s` 运行，同一个模式内部先做三步长比较，再比较控制模式。采样、controller、PLC、actuator 周期、噪声 realization、延迟和事件时刻全部固定；遇到物理步内事件精确切分，记录 nominal/实际最大子步，不能把 0.2s 采样取整成另一套控制实验。

比较 Peak temperature、Minimum headroom、Energy balance residual、Pump electrical energy、Flow、ΔP、Controller output；轨迹在共同物理时刻对齐，阶跃使用同侧取值。预注册 Generic 数值门限建议：相邻两组 dt 的峰温/最小裕量差≤0.1K，电能相对差≤1%，flow/ΔP 轨迹差≤各自预设 rated scale 的1%，执行器输出轨迹差≤1个百分点；接近零电能使用预先给定绝对 J 容差。能量累计残差≤`abs_tol_J + rel_tol×能量通量绝对积分`，每步也检查，具体尺度/容差为 PHASE 2 合同必填项。0.1→0.05 的误差应减小或已落入数值误差底线。任一关键指标越限为 **FAIL**，不能平均抵消；需要更细步长/其他积分器重新验证，不能为了 FF 胜出放宽门限。该门限是数值一致性条件，不是设备校准精度。

## 9. 开发顺序与验收门禁

| 阶段 | 交付 | 进入下一阶段的条件 |
|---|---|---|
| PHASE 1（本次） | 本计划、仓库证据和官方接口核对 | **停止，用户审查** |
| PHASE 2 | 隔离分支/目录、V0.1 manifest、`architecture.md`、下列16项合同 | 全部合同定义完整且无未决阻塞项，方可进入 PHASE 3 |
| PHASE 3 | Power→GPU/CPU chip→plate(flow)→local coolant + air paths，独立积分器 | 零输入、绝热储能、解析响应、Rth(flow)有效域、瞬态守恒、0.2/0.1/0.05s 收敛 |
| PHASE 4 | tray/branch/rack、固定体积管路、泵网络、有限 CDU/FWS | 质量平衡、等功率不均衡、泵曲线、两侧 HX 能量、全回路库存验证 |
| PHASE 5 | 本地 PID、级联环、flow/ΔP 选择、PLC 安全与执行器 | 饱和/延迟/ramp/联锁/备用或无备用降级测试，已调好的基线 |
| PHASE 6 | 确定性 FF、三模式、最大安全供液规则、watchdog | FF 可关闭、u 分量闭合、无扰回退、越界/低置信度回退 |
| PHASE 7 | 四路径 adapter、独立采样/网络/计算/PLC/VFD 延迟、noise/bias、stale/sentinel/loss | 因果性、原始 sample age、独立 RNG、延迟总账与 0.1/0.5/1s 测试 |
| PHASE 8 | 十一类场景、A/B/C benchmark、性能/保护实验隔离 | 公平 gate、匹配资格、功率 trace 不可变性、完整矩阵可重放 |
| PHASE 9 | 六份 MD、HTML、16 类图及匹配/Pareto 图、全部 KPI 与 A–Z 证据 | 没有未满足项伪标 PASS；V0.1 回归与 wheel 安装验收 |

PHASE 2 先定义 telemetry/sensor 合同，PHASE 7 才实现完整扰动链，避免 PHASE 5/6 控制器依赖真值后再拆改。工程门禁阈值预先写配置，不能看完策略结果再放宽。

### PHASE 2 Entry Gate 与 PHASE 3 Blocking Gate

PHASE 2 的进入条件是：Revision 1 完整且用户重新审查批准；本次只达到供审查的计划准备状态，不代表已获批准。PHASE 2 的交付不止 architecture.md，必须把以下 16 项写成可检查的输入、输出、不变量、错误政策和验证用例合同。下表是必交清单，**不是声称合同已经完成实现**。

| # | 必需合同 | PHASE 2 必须明确的内容 |
|---|---|---|
| 1 | Power domain contract | 电计量边界、included_in、去重、覆盖率、trace 不可变性 |
| 2 | Thermal domain contract | GPU/CPU 全功率注入、C/R、节点能量、局部温度含义 |
| 3 | Cooling path contract | liquid/air/mixed 拓扑、显式其他热路径、无重复注入 |
| 4 | Cold plate Rth(flow) contract | 本地 flow、R/UA端点、有效域、下界、零流量/域外政策 |
| 5 | Pump energy boundary | electric/hydraulic/loss/液体/环境/水力耗散的计量闭合 |
| 6 | Telemetry timestamp contract | 四 adapter、sample/receive/available、clock/age/sentinel |
| 7 | True-state / measured-state separation | 输入权限、测量通道、无真值回灌与独立随机流 |
| 8 | Performance vs Safety benchmark separation | experiment_type、trace lock、保护降载执行权限与审计 |
| 9 | Thermal-Matched benchmark definition | 同一热约束、调参白名单/预算、holdout、可行性门禁 |
| 10 | Energy-Matched benchmark definition | 泵电能边界、匹配 tolerance、不可匹配状态与比较窗口 |
| 11 | Numerical integrator interface | backward Euler候选/参考法、通量积分、dt收敛尺度与绝对/相对容差 |
| 12 | Energy control-volume definition | 外界/内部通量、储能、FWS/air/pump 边界、积分残差 |
| 13 | Mass control-volume definition | 固定库存、分支流分配、流向、节点连续性与误差容限 |
| 14 | FF / FB input permission matrix | measured IT vs thermal/liquid反馈；未来扰动日程不可访问 |
| 15 | Safety supervisor state machine | NORMAL/FF_DISABLED/DEGRADED/DERATE_REQUESTED/PROTECTED 状态、转换、迟滞、备用配置 |
| 16 | FAIR_COMPARISON hash contract | 不变量、实际输入哈希、B/C受控匹配字段、版本/seed/控制周期 |

任意一项未定义、边界互相矛盾或仍有必须填写的匹配/数值容差，即 **不得进入 PHASE 3**。PHASE 3/4 先验物理验证后，在 PHASE 8 对闭环模式再次执行 dt 收敛，不能用早期单节点收敛代替最终闭环收敛。

### Performance 与 Safety / Protection 两种独立实验

**PERFORMANCE BENCHMARK**：预生成的逐设备 IT true power trace 为 IMMUTABLE，只读供两策略使用；任何温控器、PLC 或 supervisor 不得重写它。越温、流量不足、容量不足只记录 `THERMAL_LIMIT_EVENT`、`DERATE_REQUESTED`、`COOLING_CAPACITY_INSUFFICIENT` 及其他硬件动作，保留违约时长；DERATE_REQUESTED 不触发实际 IT 功率减少。数值发散或离开模型有效域应终止/标 INVALID，不通过削减 IT 功率继续“完成”性能对比。该政策只适用于离线性能仿真，不能作为实机旁路保护的部署配置。

对 baseline 和 FF+PID 分别计算输入预期 trace 与**实际施加给热节点的功率 trace**哈希，并在相同时间网格上核验；任何策略修改 true trace，立即 `FAIR_COMPARISON=FAIL`，收益结论 INVALID。Board/domain 去重后做逐器件及总量双重检查，避免总功率相同掩盖不同设备被降载。

**SAFETY / PROTECTION BENCHMARK**：同一个不可变 offered demand，经两策略共享的 protection state machine，允许在 Synthetic IT 模型中执行 derate；实际服务功率可因此不同。记录 `derate_request_time, derate_activation_time, derate_duration, curtailed_power_w, energy_not_served_J, thermal_recovery_time`、恢复滞回与执行延迟。`energy_not_served=∫max(P_offered−P_delivered,0)dt`；它是计算侧电功率未供给量，不等同于 GPU 工作量/吞吐损失。未恢复标 censored/N/A，不记成0秒。

两实验类型的输出目录/manifest 明确 `experiment_type`，保护结果不能并入 A/B/C 节能排名。保护实验公平性比较 offered trace、保护策略、硬件和测量链；允许由相同保护规则引起不同 delivered trace，但必须披露削减量。PID/FF 对保护规则无独立调参权限。

### 三层 Feedforward Benchmark 与 Pareto 结论

三层都属于 PERFORMANCE BENCHMARK，首先通过 FAIR_COMPARISON、数值收敛、模型有效域与完整运行检查。以同样预热流程取得共同完整初态，统一评价窗口；报告最终储能差及结束状态，避免用“暂时把热存到设备里”声称能效优势。相同设施边界、测量误差 realization、采样/延迟、内环 PLC 和执行器约束适用于全部层。

| 层次 | 固定/匹配条件 | 输出与允许结论 |
|---|---|---|
| Benchmark A — Fixed Hardware / Fixed Workload | 完全相同逐设备 IT trace、hardware、FWS、noise、sensor delay、actuator constraints、PLC inner loop及共同策略参数；仅 FF 开关差异 | 全部 KPI；温度降低而耗电增加只能先记 trade-off |
| Benchmark B — Thermal-Matched | 两策略满足预注册同一个 `min headroom≥X K` 或 `peak T≤limit−margin`，同时满足其余约束 | 比较泵电能、平均泵电功率、control effort、flow、ΔP、supply T；同热安全下较低电能才支持泵冷却能效改善 |
| Benchmark C — Energy-Matched | 同窗口、同计量边界 `|E_FF−E_PID|≤max(abs_match_tol_J, rel_match_tol×E_reference)` | 比较峰温、最小裕量、IAE/ISE、variance、thermal violation duration；用于评价相同泵电能下的动态热性能 |

B 采用约束匹配而非强迫两条温度曲线逐点重合；对每种策略用同样 search budget/候选范围寻找满足同一裕量的低能耗候选。C 使用预注册能量预算；匹配失败标 `MATCH_FAILED`，不能在画图后缩放泵能耗或篡改功率 trace。近零能耗使用绝对 J 容差。所用 X、margin、absolute/relative match tolerance、调参字段、候选范围、预算、seed 和性能显著差异阈值须在 PHASE 2 填入 manifest，并在看 holdout 结果前冻结。

为解决匹配与公平性的冲突：A 冻结全部公共控制参数；B/C **仅允许预注册外层匹配旋钮**（如共同范围内的基础 ΔP/flow 或 supply-T 目标）按相同寻优程序变化，不改变 PLC 内环 gains、硬件/限值、保护或 noise/delay。策略类型及其锁定参数各自记录哈希；可调字段不从记录删除，只从公共相等性比较中按白名单排除，记录每次候选和选中理由。白名单之外有任何差异仍 FAIL。

训练/调参 workload 与 holdout workload 分开；不只挑 FF 有利场景。多 seed 成对统计使用相同扰动 realization，先给预注册有意义差异阈值及不确定区间。热约束不满足的 B 候选标 INFEASIBLE；找不到可行点本身是结果，不改变 thermal constraint。冷却总能效需要相同设施电耗边界，只有泵电能时结论限定为泵电能。

最终绘制 `Thermal Risk vs Cooling Energy` 候选集与非支配前沿，横轴明确泵电能或完整冷却电能，纵轴分别展示裕量短缺积分/越温时长及最小裕量，不混成未经定义的单分数。允许多标签并附对应 KPI：

- **FF improves thermal response**：优先由 C 中满足能量匹配且超过显著差异阈值的 thermal KPI 支撑；A 的改善同时披露能耗变化。
- **FF reduces energy**：由 B 同热约束下的较低泵/冷却电能且其余硬约束成立支撑。
- **FF shifts Pareto frontier**：同一候选/搜索预算下存在稳健非支配改进；结论仅限已测试范围。
- **No meaningful benefit**：差异未超过预注册实用阈值/数值与随机不确定性，不能等同于证明永远无收益。
- **Trade-off only**：热指标改善但电能/控制努力增加，尚无匹配或 Pareto 改进证据；若 FF 被基线支配，直接报告退化，不包装为收益。

### PHASE 8 场景矩阵

| 场景 | 必测变体 | 关注点 |
|---|---|---|
| STEP | 30→90→30；另保留 V0.1 30→95→30 兼容场景 | 等起点、热安全/能耗 trade-off |
| RAMP | 30s / 60s | 设定值变化与 actuator limit |
| BURST | 5s 高功率、10s 低功率重复 | 采样漏检、积分累积、动作频率 |
| RANDOM | 固定 seed、多 seed 扰动 | 稳健性和置信区间 |
| ASYMMETRIC | 全部50%额定功率 vs 1/4满功率、其余约1/3，按实际 W 精确配平 | 局部最坏温度、支路流量和压差 |
| POWER TRACE | CSV/JSON，逐 GPU/CPU，断点/重复/乱序/缺测 | 计量域去重、无未来泄漏 |
| TELEMETRY DELAY | 0.1/0.5/1/2s；与采样周期分别扫描 | FF 的收益消失点也必须报告 |
| ACTUATOR LIMIT | 同时限制两策略 ramp/容量 | 不允许 FF 独占更快硬件 |
| FAULT | flow bias、温度噪声、泵延迟、遥测丢失、模型无效 | 可观测故障、FF 回退、硬约束事件 |
| Scenario 10 — FWS DISTURBANCE | IT trace不变；FWS 20→24°C；另测流量100%→70% | 上游热边界/容量降级，只经测量影响 FB；FF不得预知事件 |
| Scenario 11 — BRANCH HYDRAULIC DISTURBANCE | IT trace不变；指定支路 K→1.5K | blockage/软管/快接/阀异常；报告局部flow/GPU温度/支路ΔP和rack总flow |

Scenario 10/11 的事件日程只提供给 plant/scenario injector，不提供给 FF estimator；流体扰动通过有延迟/噪声的传感器链进入 FB 和 protection。IT 功率不变时 FF 的 IT 驱动分量不应提前响应隐藏扰动。测试验证 FB 对偏差作合理方向的纠偏；能否恢复取决于硬件可行域，容量不足则正确报警/降级即是保护行为，不能要求凭空恢复到原设定值。性能实验仍不降 IT 功率，另有独立保护实验时才执行降载。这两类场景用于区分 IT-side measured disturbance FF 与 cooling-side unknown disturbance FB，而非保证 FF-only 能克服未知水侧变化。

### FAIR_COMPARISON 与 KPI 合同

硬件、拓扑、FWS/branch 扰动计划、初始完整状态、IT offered trace、PERFORMANCE 下的实际 true trace、采样/延迟/noise 配置与实际随机序列、PLC 内环调参、积分器及调度配置做规范化哈希。纳入 `experiment_type, benchmark_class, schema/code/solver_version`。A 仅允许模式/FF 开关差异；B/C 仅额外允许上述匹配白名单，并单独验证 MATCH 状态；SAFETY 允许由同一保护逻辑产生不同 delivered power并保留审计。出现未授权差异输出 `FAIR_COMPARISON=FAIL` 并将收益对比判 INVALID。**不要求两策略实际 flow/temperature 或有噪测量值相等**，只要求其源扰动/噪声 realization 和测量机制相同。任一控制器改变 PERFORMANCE IT true trace 无条件 FAIL。

KPI 包含用户要求的全部类别：

- Thermal：逐 GPU 最坏及整架统计的峰值/平均、headroom min/average/P5、低裕量时长、overshoot、settling、variance、IAE/ISE，增加 Thermal Margin Excess 与 Excess Cooling Integral。匹配比较中的 T_target/RequiredHeadroom 在策略间固定，不能各用自己的移动目标降低 IAE；移动设定值跟踪误差单列。随机/burst 无定义的 settling 标 N/A。
- Cooling：供回温、Rack ΔT、峰值/平均流量与 ΔP，逐支路最小流量/最大温度；有符号焓差保留方向。
- Energy：Pump Electrical/Hydraulic Power、Efficiency、Heat to Liquid、Cumulative Pump Electrical Energy，以及均值/峰值电功率；按第2.7节避免重复计量。设施能耗仅在有明确边界和配置模型时报告。
- Control：FF/PID/requested/accepted/actual 分量、总变差 `Σ|Δu|`、动作次数（带死区）、速率与饱和时间；不能用步间标准差冒充振荡检测。
- Reliability：约束/遥测丢失/thermal/flow/pressure 的事件次数、累计时长、最大幅度、回退原因；区分一次持续事件和多个采样点。
- Physics：用户指定 IT-water gap 与独立储能守恒残差的 instantaneous/average/P95/max，时间积分误差和各节点质量平衡。

16 类图全部使用统一时间基准和 SI/明确显示单位，覆盖 workload/power、power vs estimated heat、GPU temperature、headroom、supply/return、ΔT、flow、ΔP、pump speed/power/cumulative energy、FF、PID、total output、energy residual、baseline comparison；分支图展示最坏局部状态。报告首页区分 Official / Configured / Assumption / Synthetic；不将泛型设备模型包装成经过校准的 GB300 产品性能。

### A–O 验收映射（规划，当前均非 V0.2 PASS）

| 项 | 验证证据 |
|---|---|
| A | utilization 改变但功率 trace 固定时，热输入不变 |
| B | 各器件功率、存热、传热与冷却液焓通量完整对应 |
| C | 单元、机架、全回路瞬态积分守恒 + 合格稳态窗口 |
| D | 两模式相同 ramp/min/max/容量/lag/联锁，超出能力显式失败 |
| E | 多周期延迟事件账本，原始时间戳不被刷新掩盖 |
| F | 测量误差统计与 seed 重现；控制器不可访问真值 |
| G | FF 与 FB 输入隔离、分量日志、开关测试 |
| H | 所有建议必须经过 PLC，越界注入不能绕过 safety |
| I | 丢失/过期/异常/低置信度/越限→FF 禁用和本地控制 |
| J | N GPU/N CPU、tray、branch、rack 配置可运行 |
| K | 同功率不同分布产生可解释的局部温差；质量平衡成立 |
| L | PID、FF-only、FF+PID 都可运行，主要比较前后两种闭环 |
| M | 硬件/初态/输入/测量配置不一致时自动拒绝有效性结论 |
| N | 上述 thermal/cooling/energy/control/reliability/physics KPI |
| O | 每个配置值可追源、数据标记明确、校准状态不会自动升级 |

### P–Z 新增验收映射（规划，未实现/未执行）

| 项 | 验证证据 |
|---|---|
| P | GPU/CPU 全功率进节点；改变热阻/空气边界时散热分配由网络变化，主动态热流不依赖固定 capture fraction |
| Q | 本地flow对应cold plate Rth/UA有合理单调性和下界；有效域、零流量和厂家曲线条件已验证 |
| R | Thermal-Matched B 的共同热约束、可行性门禁、对称调参和能耗比较可重放 |
| S | Energy-Matched C 的泵电能边界、匹配容差及失败状态明确，thermal KPI可重放 |
| T | 性能实验 IT true trace 与预生成输入逐设备相同；注入一次实际功率修改会令FAIR_COMPARISON FAIL |
| U | 保护实验可实际降载，性能实验仅记录请求；共同状态机和全部削减/恢复指标可审计 |
| V | Direct DCGM 控制路径与Exporter监测路径分开；未知sample time不会被receive time冒充 |
| W | 0.2/0.1/0.05s dt 下全部规定 KPI 与守恒满足预注册收敛门限，其他周期不变 |
| X | 泵电功率、水力功率、分项损耗、液体/环境及水力耗散闭合，无双重热注入/电能计量 |
| Y | FWS温升/流量下降通过测量进入FB；FF无未来日程泄漏；可行恢复或正确容量不足状态 |
| Z | K_branch×1.5造成合理局部flow/温度变化，质量守恒；FB/保护行为和不可行状态均有证据 |

## 10. 风险与应对

| 风险 | 应对 |
|---|---|
| 聚合 R/C 迁到逐芯片失真 | 按热域重新参数化，优先解析/测量锚点，禁止除以数量后直接认定真实 |
| 传感器/模型未标定却宣称“真实孪生” | 实现验收与设备校准验收分开，未测参数保持 CALIBRATION_REQUIRED |
| 变流量管路导致数值能量漂移 | 同一界面质量/焓通量、CFL/稳定性子步、积分残差监测 |
| FF 利用全知真值或更好硬件获胜 | 输入权限和公平 gate；模型失配、延迟及不利案例也纳入 |
| 不可同时满足温度/流量/压力限制 | 可行域先验检查、运行时告警；PERFORMANCE 仅记录降载请求，独立 SAFETY 才执行降载；不把实际值裁成安全值 |
| 热量重复分配或泵热重复注入 | 逐 power domain 唯一入账、显式液/空气路径及电/水力/热控制体账本 |
| 冷板曲线越域、积分步长造成虚假收益 | 流量有效域门禁及固定其他时钟的 dt 收敛测试，失败不得发布收益结论 |
| 匹配 benchmark 挑选有利参数 | 预注册匹配容差、对称搜索预算、完整候选记录与 holdout 验证 |
| baseline 未调好导致误判 | 独立调参工况与 holdout 工况，报告参数与多 seed 结果 |
| 功率 board/device 重复计量 | 覆盖域清单、UUID/实体去重、缺失设备覆盖率 |
| Windows/打包/路径/字体破坏 V0.1 | 双包双 CLI、legacy CI、独立 wheel、中文字体 QA |
| 参数拟合不唯一 | 稳态和动态实验分开；R/C/K/UA 用多工况辨识、留出验证 |
| 需求跨度大 | 严格按 PHASE gate，不以所有接口文件存在代替物理/控制完成 |

## 11. 缺失的厂家参数

当前无实际 CDU/冷板/机架 FAT 数据。至少缺：泵 H-Q-η-N 图、功率计量边界、允许流量/压差、最小连续转速、VFD 传递函数/死区、阀 Cv 与迟滞、HX UA/容量随两侧流量温度变化图、CDU/管道/manifold 容积、逐支路阻力、接头/冷板 ΔP、FWS 波动边界、液体配方、保护顺序和冗余配置。

还缺冷板 flow–Rth/UA 曲线及其测试边界、仪表精度与测试不确定度，电机/VFD/泵内部效率和损耗去向。不能以总效率推定全部电气损耗进入液体；具体数据包见第13节。

这些参数缺失不阻止建设 Generic 模型，但阻止宣称具体厂家性能或现场安全控制已经验证。

## 12. 缺失的 NVIDIA / 服务器参数

缺逐 GPU/CPU 功率域定义与真实 trace、有效功率上限、实际热降频/关机阈值及温度量定义、GPU/CPU/HBM 冷却分配、设备级 R/C、冷板接触条件、tray/branch 物理对应关系、Grace 数据源实体支持、采样平均窗口、DCGM/driver/firmware 支持矩阵。

其中冷却分配是待校准的网络结果/稳态指标，不是替代 GPU/CPU RC 主路径的固定比例。还需实测空气热路径、源采样时间戳可用性、真实硬件更新/平均周期、端到端控制遥测延迟及实际 throttling trace。

官方产品页确认 GB300 NVL72 包含 72 个 Blackwell Ultra GPU 与 36 个 Grace CPU，并采用液冷机架架构；这不提供本模型所需的 R/C、CDU、支路和功率曲线，也不能用“液冷”推定所有电功率 100% 进入所建水路。[NVIDIA GB300 NVL72](https://www.nvidia.com/en-us/data-center/gb300-nvl72/)

## 13. 建议从 CDU 厂家获取的数据

要求带设备型号/序列号/固件/测试日期、冷却液配方、仪表精度、校准证书、采样周期和时间同步说明的 CSV/JSON 原始数据，而非只有 PDF 选型点。

| 数据包 | 最少内容 | 用途 |
|---|---|---|
| 稳态 sweep | 多转速、多阀位、两侧流量与进出温度、绝压/ΔP、泵电功率 | K、泵效率、Cv、UA/容量可行域 |
| 动态 step | 时间对齐的 command/accepted/actual speed/valve、flow/ΔP/T | 执行器 delay/lag/ramp、容积和传热动态 |
| 拓扑与体积 | 供回管长/内径、manifold、CDU 水容积、branch/冷板连接 | 固定质量、branch 分流和 residence time |
| 故障/联锁矩阵 | 流量丢失、低压、高压、泵故障、备用切换、恢复逻辑 | safety 状态机、故障注入与验证 |
| 接口与定义 | Modbus/BACnet/OPC UA 映射、读写权限、单位、刷新/超时 | 未来厂商 adapter，确认 absolute vs differential pressure |
| 泵驱动能量包 | VFD输入/输出、电机轴功率、泵压升/流量的同步数据；各级效率与损耗流向、液冷/风冷结构及不确定度 | 辨识motor/VFD/internal loss及液体/环境分配，校核第2.7节边界 |

### Server/Cold Plate Package

下列数据向服务器 OEM/冷板厂家联合索取；不同 GPU/CPU 型号不得共用未经验证的曲线。

| 项 | 必需数据及边界 |
|---|---|
| 1 | GPU cold plate Flow vs ΔP，明确压差取压位置 |
| 2 | GPU cold plate Flow vs Thermal Resistance，明确温度测点与热阻端点 |
| 3 | CPU cold plate Flow vs ΔP，明确压差取压位置 |
| 4 | CPU cold plate Flow vs Thermal Resistance，明确温度测点与热阻端点 |
| 5 | Coolant formulation：介质、浓度、温变ρ/Cp/μ/k及添加剂条件 |
| 6 | Inlet temperature：各曲线对应入口温度和适用温区 |
| 7 | Maximum/minimum qualified flow：合格有效域、零/低流量限制 |
| 8 | Test heat load：施加功率、空间分布与稳态判据 |
| 9 | Test uncertainty：仪表精度、校准状态及流量/温差/功率组合不确定度 |
| 10 | TIM/contact condition：材料、厚度、接触压力和安装条件 |
| 11 | Device-to-coolant thermal resistance：die/package/plate至bulk coolant各端点，禁止重复加入TIM/接触热阻 |
| 12 | Tray branch topology：串并联、器件与支路对应关系 |
| 13 | Manifold distribution：流量分配、阻力、几何/体积及不均匀性 |
| 14 | Local flow sensor位置（如有）：量程、精度、刷新周期与实际覆盖支路 |
| 15 | Actual thermal throttling test trace（如可获取）：同步功率、温度、clock、限制原因、激活/恢复时间 |

附加索取 Flow vs UA / heat transfer coefficient（如果厂家提供），连同有效面积、热流/温差定义和相同介质、入口温度、热负荷条件，不能直接把不同端点的UA与device Rth互换。缺少上述校准证据时 Generic Model 可以运行，但 `CALIBRATION_STATUS` 必须保持 `UNVALIDATED` 或 `CALIBRATION_REQUIRED`；可选数据不可获取须记录原因及验证范围限制，测试通过不自动升级为设备已校准。

校准接口仅规划 schema/loader 与参数候选输出：输入数据集哈希、工况、sensor uncertainty、候选参数/上下界/拟合残差、留出残差、版本和人工接受状态。不得自动覆盖已验收参数。

## 14. 建议从 NVIDIA/DCGM 获取的数据与官方核对

以下保留 Revision 0 在 2026-09-16 记录的官方页面核对证据，Revision 1 未重新在线验证；`latest` URL 是可变页面，实施阶段必须锁定目标 DCGM、Exporter、driver 和设备版本。**本阶段不写任何数字 field ID 到程序。**

来源：

- [NVIDIA DCGM Field IDs](https://docs.nvidia.com/datacenter/dcgm/latest/dcgm-api/dcgm-api-field-ids.html)：当前读取页列出 BOARD_POWER_WATTS、GPU_TEMP_CELSIUS、GPU_UTIL_RATIO、CPU_POWER_WATTS 等名称。
- [NVIDIA DCGM Exporter](https://docs.nvidia.com/datacenter/cloud-native/gpu-telemetry/latest/dcgm-exporter.html)：示例仍使用 POWER_USAGE、GPU_TEMP、MEMORY_TEMP 等 Exporter 名称；包含 collector 配置、UUID 标签与不支持值示例。页面给出的采集间隔默认值为 30000ms，不能假定现场默认达到 100ms/1s。
- [NVIDIA GB300 NVL72](https://www.nvidia.com/en-us/data-center/gb300-nvl72/)：用于官方平台组成核对，不用于替代 OEM 热工参数。页面完整导航曾超时，但读取到正文及 configuration 表。

| 标准字段/需求 | 本次官方核对结果 | V0.2 接入策略 |
|---|---|---|
| `gpu_power_w` | 字段页 `DCGM_FI_DEV_BOARD_POWER_WATTS`；Exporter 示例 `DCGM_FI_DEV_POWER_USAGE` | 版本化别名，验证 board/device 边界及内部平均窗口，不再叠加其已包含的 HBM/board 功耗 |
| `gpu_util_pct` | 当前字段页 `DCGM_FI_DEV_GPU_UTIL_RATIO` | 不能凭名称猜 ratio/percent；按目标版本元数据/实测样例明确转换 |
| `gpu_temp_c` | 字段页 `DCGM_FI_DEV_GPU_TEMP_CELSIUS`；Exporter `DCGM_FI_DEV_GPU_TEMP` | 温度为反馈；确认 die/hotspot 定义，不把聚合温度当实测结温 |
| memory temperature | 字段页 `DCGM_FI_DEV_MEMORY_TEMP_CELSIUS`；Exporter `DCGM_FI_DEV_MEMORY_TEMP` | 可空，拒绝巨大 sentinel；缺失不填成0°C |
| clock | `DCGM_FI_DEV_SM_CLOCK`，Exporter 示例单位 MHz | 单位/设备标识入标准结构 |
| thermal violation | `DCGM_FI_DEV_THERMAL_VIOLATION`，官方描述为 ns | 识别累计计数、重置/回绕；不能直接当布尔 slowdown |
| power violation | `DCGM_FI_DEV_POWER_VIOLATION`，官方描述为 ns | 与热事件区分，按设备能力选择 |
| CPU power/util | 字段页 `DCGM_FI_DEV_CPU_POWER_WATTS`、`DCGM_FI_DEV_CPU_UTIL_TOTAL` | Grace 实机/版本/实体支持待验证；不能承诺现有 Exporter 配置已导出 |
| memory activity | 候选 profiling/内存利用率字段需按目标版本进一步核验 | 不将 memory used bytes 等价成带宽活动率；未核实前保持 optional |

官方字段目录有条目不等于每个 GB300/Grace 都能返回有效值。live adapter 初期只读，启动时进行 capability discovery；默认运行仍支持 Synthetic 和已记录 trace，不依赖 GPU 库导入成功。

统一 `TelemetryRecord` 至少包含：`schema_version, source_type, source_version, entity_type, device_uuid, rack_id, tray_id, branch_id, sample_time, receive_time, available_time, value, unit, quality, sequence_id, averaging_window, power_domain, thermal_domain`；功率/利用率/温度/时钟/活动率作为具名可选字段。额外记录 inventory 与 power-domain coverage、MIG/分区标签，避免把同一板卡标签重复相加。Direct DCGM/Hostengine 是待验证的低延迟控制路径；Exporter 是监控/回放/集成路径，不默认承担快速控制。两者都必须保留真实源sample time，未知时标注质量并回退，不能用receive time冒充；缩短scrape周期不保证新的硬件样本。

厂商/运维侧优先提供：完整 inventory、DCGM/driver/Exporter 版本与 collector 配置、连续带时间戳功率/温度/限制/clock trace、Grace/板级/BMC/PDU 的同步数据、字段缺失与 sentinel 样本、功率平均窗口和真实刷新速率、负载任务起止标签。控制模型从这些测量估计热，不能反向读取仿真真值。

---

## 15. Revision 1 自审与停止条件

本次自审仅针对计划完整性和内部一致性，不是实现验收或设备验证：

1. **物理与边界：**V0.1固定热分配仅作历史说明；V0.2全功率进设备节点，RC网络决定液/空气热流，冷板有flow依赖和有效域；泵电、水力及热量分账且禁止重复注入。
2. **比较与权限：**A/B/C及Pareto分别定义；B/C匹配白名单解决调参与公平哈希冲突；PERFORMANCE实际IT trace不可修改，SAFETY独立；FF不能读取未来水侧扰动或plant真值。
3. **架构与验证：**四类telemetry路径和时间戳合同一致；积分器候选、默认选择依据、五时钟及三档dt测试已列出；Scenario10/11与P–Z补齐，A–O保留。所有测试仍是待实现验收要求。
4. **保留与缺口：**V0.1源代码、CLI、既有测试/报告能力均未修改；V0.2隔离设计、拓扑、有限体积、水力、HX、PLC、守恒、溯源/校准及阶段门禁保留。厂家曲线和GB300实测缺口仍显式标记，不将Generic结果升级为已校准设备性能。
5. **门禁结论：**已具备提交用户复审、申请进入PHASE 2的计划层面条件；用户再次批准仍为必要条件。16份合同的具体字段、参数、容差与审查证据是PHASE 2产出，当前不宣称已完成；任一未定义不得进入PHASE 3。

**本阶段停止点：**仅交付本文件的Revision 1；未创建分支、未修改V0.1、未编写V0.2实现、未启动PHASE 2。停止并等待用户重新审查和明确批准。
